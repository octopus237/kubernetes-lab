apiVersion: v1
kind: Service
metadata:
  name: {{ include "ls-lab04-st9.fullname" . }}
  labels:
    {{- include "ls-lab04-st9.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ls-lab04-st9.selectorLabels" . | nindent 4 }}

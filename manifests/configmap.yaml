apiVersion: v1
kind: ConfigMap
metadata:
  name: db-config
data:
  MONGO_INITDB_ROOT_USERNAME: 'admin'
  MONGO_INITDB_ROOT_PASSWORD: 'password'
  MONGO_DB_ADD: 'mongo-service.default.svc.cluster.local'
